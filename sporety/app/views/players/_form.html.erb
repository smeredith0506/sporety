<%= form_for(@player) do |f| %>
  <% if @player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
      <% @player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_types %><br>
    <%= f.text_field :game_types %>
  </div>
  <div class="field">
    <%= f.label :availabilities %><br>
    <%= f.datetime_select :availabilities %>
  </div>
  <div class="field">
    <%= f.label :matches %><br>
    <%= f.number_field :matches %>
  </div>
  <div class="field">
    <%= f.label :teams %><br>
    <%= f.text_field :teams %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
